services:
  taskapi:
    image: ${IMAGE_TAG}

    container_name: taskapi

    restart: unless-stopped

    ports:
      - "8081:8080"

    environment:
      SPRING_PROFILES_ACTIVE: prod

    networks:
      - taskapi-network

networks:
  taskapi-network:
    driver: bridge
